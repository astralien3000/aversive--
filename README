POUR COMMENCER

À la racine du projet, faire make doc pour générer la documentation.

Ouvrir le fichier build/doc/html/index.html avec n'importe quel navigateur.

//////////////////////////////////////////////////////////////////////////////////
POUR EXECUTER TOUT LES TESTS UNITAIRES

make test

///////////////////////////////////////////////////////////////////////////////////
commandes utiles (pour mémoire)

avr-g++ -std=c++11 -mmcu=*** -I./include/

simulavr -f a.out -d  atmega** -R 0xFA -W 0xFF

avr-objcopy -O ihex *.elf *.hex
sudo avarice -j /dev/ttyUSB0 --erase --program -f *.hex

TODO :
Parseur : le dernier registre n'est pas parsé
